#include <stdio.h>

// loop dentro de loop

int main()
{
    int rows;
    int columns;
    char symbol;

    printf("Enter the number of rows:\n > ");
    scanf("%d", &rows);
    printf("Enter the number of columns:\n > ");
    scanf("%d", &columns);
    printf("Enter a symbol:\n > ");
    scanf("%c", &symbol);


    return 0;
}
